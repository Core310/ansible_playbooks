# Example: Session Checkpointing and Context Clearing

```bash
python3 /home/arika/Documents/ansible_stuff/ansible_playbooks/fresh/desktop/configs/gemini/config/skills/cta-init/scripts/cta_engine.py --workspace . checkpoint \
  --milestone "M001: Architecture" \
  --phase "Phase 3: Service Layer" \
  --task "Task 3.2: Implement Order Processor" \
  --next "Write unit tests in tests/test_order.py and run pytest."
```
